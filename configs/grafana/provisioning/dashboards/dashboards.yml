# Grafana dashboard provisioner（Day 10）
#
# 让 Grafana 自动从 /var/lib/grafana/dashboards/ 加载所有 JSON。

apiVersion: 1

providers:
  - name: slink
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /var/lib/grafana/dashboards
