# 二进制 / 构建产物
/bin/
/build/
/dist/
/slink
/server
# 仅忽略仓库根的 slink-* 二进制 / 包；不要写 slink-*（会误伤子目录如 configs/grafana/dashboards/slink-overview.json）
/slink-*
# 同理 spike build 产物（cmd/spike-* 子目录的 main.go 是源码，不会被忽略）
/spike-*

# Go 测试 / 覆盖率
*.test
*.out
coverage.txt
coverage.html
profile.out

# 环境变量
.env
.env.local
.env.*.local

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# 临时
tmp/
*.log

# Docker volumes 本地导出
data/

# 压测产物
*.lua.out
bench-results/

# 私人开发日志（本地保留 / vault 归档，不入公开仓）
docs/journal/
docs/walkthrough/
docs/retrospect/
